#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_watchr@2.4.13@watchr/bin/watchr" "$@"
  ret=$?
else 
  node  "$basedir/../../../_watchr@2.4.13@watchr/bin/watchr" "$@"
  ret=$?
fi
exit $ret
