#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_messageformat@0.3.1@messageformat/bin/messageformat.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_messageformat@0.3.1@messageformat/bin/messageformat.js" "$@"
  ret=$?
fi
exit $ret
